<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.guevarag.apprendrejouer">

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IdentificationActivity" />
        <activity android:name=".NouveaujoueurActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".SousMenu.CultureActivity" />
        <activity android:name=".SousMenu.MathActivity" />
        <activity android:name=".SousMenu.HistoireActivity" />
        <activity android:name=".SousMenu.ResultatActivity" />
        <activity android:name=".OperationActivity"></activity>
    </application>

    <meta-data
        android:name="DATABASE"
        android:value="apprendre_jouer.db" />

    <meta-data
        android:name="VERSION"
        android:value="1" />

    <meta-data
        android:name="QUERY_LOG"
        android:value="true" />

    <meta-data
        android:name="DOMAIN_PACKAGE_NAME"
        android:value="package_entites.data" />


</manifest>